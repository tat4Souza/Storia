export default function LogoComplete({ className }: { className?: string }) {
  return (
    <img
      width={350}
      className={className}
      src="/img/logo_complete.png"
      alt="Logo"
    />
  );
}
