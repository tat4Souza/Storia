export default function LogoLetters({ className }: { className?: string }) {
  return (
    <img
      width={350}
      className={className}
      src="/img/logo_letters.png"
      alt="Logo"
    />
  );
}
